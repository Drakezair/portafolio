import React from 'react';
import { render } from 'react-dom';
import Certificados from '../container/Certificados';

var container = document.getElementById('container');

render(<Certificados />, container)
